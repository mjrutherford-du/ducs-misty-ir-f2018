from helpers import getAndViewMap

'''
Just so can easily from the command line just call this if want to see the map, rather then needing to be subscribed to 
the self state also.
This will do nothing if there isn't a map to get.
So assumes there is a map to get and display.
'''

getAndViewMap(timesBigger=2)
